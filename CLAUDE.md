# Cloth Simulation — контекст для Claude Code

Этот файл читается автоматически при каждом запуске сессии в этой папке.

## Быстрый старт

Читай `AGENT.md` — там полная карта проекта: архитектура, что отложено, что нельзя трогать.

## Сборка и запуск

```bash
cd build && make -j$(nproc) && ./cloth_sim
```

## Текущая ветка

`master` — один коммит, всё рабочее.
